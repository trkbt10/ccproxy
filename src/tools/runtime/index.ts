/**
 * Tool Runtime System
 * 
 * Main entry point for the tool runtime system that manages
 * builtin, dynamic, and external tools.
 * 
 * Import modules directly:
 * - Types: import from "./types"
 * - Registry: import from "./registry"
 * - Manager: import from "./manager"
 * - Interceptor: import from "./interceptor"
 * - Processor: import from "./processor"
 * - Adapters: import from "./adapters/generation-to-runtime"
 */

// This file intentionally left without barrel exports to follow project conventions.
// Import directly from the specific modules as shown above.